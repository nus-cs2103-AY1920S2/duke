plugins {
    id 'java'
    id 'application'
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.5.0'
    compile 'junit:junit:4.12'
}

test {
    useJUnitPlatform()
}

group 'seedu.duke'
version '0.1.0'

repositories {
    mavenCentral()
}

application {
    // Change this to your main class.
    mainClassName = "duke.main.Main"
}

run {
    standardInput = System.in
}
