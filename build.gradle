plugins {
    id 'java'
    id 'application'
}

group 'lcduke.Duke'
version '0.1.0'

repositories {
    mavenCentral()
}

application {
    // Change this to your main class.
    mainClassName = "lcduke.Duke"
}

run {
    standardInput = System.in
}