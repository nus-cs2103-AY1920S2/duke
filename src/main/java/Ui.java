/**
 * Prints and formats Duke's response to user input.
 */
public class Ui {
    public void print(String toPrint) {
        String line = "\n______________________________________ \n";
        System.out.println(line + toPrint + line);
    }
}