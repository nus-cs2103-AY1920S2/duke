package commands;


/**
 * Enum to hold the different types of commands available to be created.
 */
public enum CommandType {
    HI, BYE, LIST, LISTON, DONE, TODO, DEADLINE, EVENT, DELETE, INVALID
}
