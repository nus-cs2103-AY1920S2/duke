package processor;

/**
 * Handles the printing of content to the user interface (UI).
 */
public class Ui {

    /**
     * Prints output to the UI.
     *
     * @param output Output to be printed to the UI.
     */
    public static void print(String output) {
        System.out.println(output);
    }
}
